# “收尾”使用指南（Windows 11）

“收尾”是一道提前承诺的减速带：门禁后阻止新娱乐，给已经开始的游戏或视频留出安全善后时间，最后锁定 Windows。它防的是临时冲动，不防拥有管理员权限的用户主动停用或卸载，也不诊断或治疗失眠。

## 安装前确认

- 电脑是 x64 Windows 11，操作系统内部版本不低于 22000。
- 首版按一台电脑、一个 Windows 登录账户、一个 Chrome 用户资料和一部可选的 iPhone 设计。
- 先保存正在编辑的文件。安装不会主动关机、断网或修改电源计划。
- 当前 MSI 未签名，因此 Windows 可能显示“未知发布者”。只使用项目交付的 `NightGate-x64.msi`，并先核对同目录 `.sha256` 文件中的 SHA-256；不要绕过来自其他来源文件的安全提示。
- 如果电脑上还有旧版安装窗口，先关闭它，再使用最新 MSI。

## 安装与第一次打开

1. 双击 `NightGate-x64.msi`，在 Windows 的账户控制窗口中确认安装。
2. 安装完成后，可从桌面的“收尾”、开始菜单的“收尾 NightGate”或通知区域托盘图标打开。注销并重新登录后，程序也会随当前账户启动。
3. “收尾”平时缩在任务栏通知区域。找不到图标时，先点任务栏右侧的 `^`；双击图标或右键选择“打开收尾”。
4. 重复运行桌面程序不会再建立第二套后台保护，而会唤起已经运行的窗口。

安装程序会建立一个以 LocalService 身份运行的 Windows 后台服务，并为当前 Windows 账户登记托盘端和 Chrome 本地通信宿主。程序自带运行环境，不要求另外安装 .NET。

若已安装 0.1.0、0.2.0、0.2.1、0.2.2、0.3.0、0.3.1、0.3.2、0.3.3、0.3.4、0.3.5、0.3.6、0.3.7、0.3.8、0.3.9、0.3.10、0.3.11、0.3.12、0.3.13、0.3.14、0.3.15 或 0.3.16，先从托盘正常退出旧桌面端，再直接双击 0.3.17 MSI 覆盖安装。升级会保留 `C:\ProgramData\NightGate` 中的当前夜间状态、设置和历史。若 Windows 提示文件正在使用或要求重启，请先退出旧托盘进程并按提示重启，再打开“收尾”。

## 完成首次设置

第一次打开会进入可返回、可续接的五步向导：

1. 作息与旧任务：确认当前作息台阶，并逐项检查旧的自动关机计划任务。只有你勾选并确认的任务才会被停用，程序不会删除它们。
2. 程序与网站：添加受限游戏、关联辅助程序和语音工具，并从当前支持的网站列表中选择要保护的娱乐网站；为每个游戏设置 15–90 分钟的典型一局时长。
3. Chrome 网页保护：加载扩展后，打开扩展网站权限设置，选择与“程序与网站”页面相同的网站并允许访问，再确认扩展和隐身模式状态。扩展不可用时，只有明确接受网页保护降级后才能继续，最终 Windows 锁屏仍会执行。
4. iPhone 清单：逐项确认睡眠计划、睡眠专注、停用时间、必要工具白名单和手机放置位置。
5. 保护边界：确认“防冲动，不防蓄意拆除”后完成向导。

服务暂不可用时会显示可重试的安全放行页面：电脑保持可用，向导不会拿旧缓存继续保存；服务恢复后点击重试即可续接。完成向导后，“设置”按“作息与晋级、程序与网站、Chrome 网页保护、iPhone 清单、旧任务/历史与隐私”分类显示。22:30 前的规则修改可用于当晚，更晚的修改安排到次日。

停用旧自动关机任务时，程序会在写入后重新读取 Windows 的实际状态；只有 Windows 确认已经停用才会显示成功。若任务由管理员创建，系统会弹出一次账户控制确认，请核对操作来自“收尾”后选择“是”。若取消，任务不会被当成已停用，可重新勾选再试。

## Chrome 网页保护

首版需要手工加载随程序安装的 Chrome 扩展：

1. 在 Chrome 地址栏打开 `chrome://extensions`。
2. 打开右上角“开发者模式”，选择“加载已解压的扩展程序”。
3. 选择 `C:\Program Files\NightGate\chrome-extension`。
4. 回到“收尾”的 Chrome 网页保护页面，点击“打开扩展网站权限设置”。在打开的扩展选项中，选择与“程序与网站”页面相同的网站，点击“保存并授权”，并在 Chrome 提示中允许访问这些网站。仅加载扩展但没有完成这一步时，网页保护仍会降级。
5. 如果按钮没有打开页面，在 `chrome://extensions` 找到“收尾”，依次打开“详细信息”→“扩展程序选项”。
6. 回到“收尾”点击“重新检测”，确认显示扩展已连接。
7. 如果需要保护隐身窗口，在扩展“详细信息”里允许无痕模式；否则在向导中确认你知道隐身模式暂未保护。

0.3.17 的 Chrome 扩展仍为 `0.1.5`，本次只更新桌面浮窗；已经是 0.1.5 时无需重新授权。若仍显示 0.1.4，请在 `chrome://extensions` 的“收尾”卡片上点击一次“重新加载”，再回到桌面端确认版本为 `0.1.5`。原有网站授权会保留，无需重新勾选；旧扩展退出后的核心安全放行仍有效，但已经显示的拦截页可能最多等待旧的 45 秒租约。

扩展只在你选择的网站类别上判断媒体状态，不保存页面标题、完整网址或完整浏览历史。扩展缺失、关闭或通信失败时，界面会显示“网页保护降级”；最终 Windows 锁屏仍由桌面端执行。

## iPhone 同步

Windows 程序不能远程修改 iPhone。每次晋级时，请按设置页清单手工同步“健康”睡眠计划、睡眠专注和“屏幕使用时间 → 停用时间”，并打开“停用期间阻止使用”。

电话、时钟、必要联系人、验证器、交通、支付和健康工具应保持允许；Safari 不要加入“始终允许”。屏幕使用时间密码应不同于设备解锁码，同时保留 Apple 的恢复方式。到手机放置时间后，把手机放在必须起身才能拿到的位置。

## 每晚会发生什么

- `Free`：受保护的无负罪娱乐时间，不反复提醒。21:00 只是内部保护窗口的起点，不再弹通知；“如果—那么”计划卡只在有效最后开局前 30 分钟出现一次。若某个已生效的游戏规则需要更长的一局时间，计划卡会随它的最早截止时间一起提前。
- `LastStart / Grace`：每个游戏达到自己设置的一局时长所对应的截止时间后，才会禁止新开该游戏；截止前已经运行的游戏可以善后。受保护网站允许当前视频结束，但阻止下一集或新的娱乐页面。
- `LandingLocked`：到点锁定 Windows；不会主动断网、强杀门禁前的游戏或改电源计划。重新登录后若仍在限制期，会只保留例外入口并再次锁屏。
- `Morning`：到起床时间后自动解除限制，并显示简短结果。

从 0.3.16 开始，到了“最后开局”的收尾提醒时间，如果你配置过的游戏还开着，会出现一张更大的浮窗：约 560×240 的界面尺寸，比“再次使用”等例外中的 380×148 卡片更醒目，实际像素大小随 Windows 缩放比例变化。它会同时显示当前时分秒、锁屏时间和剩余倒计时。自定义为较长一局的游戏会随最后开局提醒提前显示；更早的“如果—那么”计划卡不会触发它。

程序每 3 秒只读检查一次当前账户、当前会话里配置为“游戏”的主程序。只开着语音工具、关联辅助程序，或另一目录里同名的程序，不会触发大号浮窗。关闭游戏后，就恢复原来的锁屏前最后 10 分钟卡片规则；最后 2 分钟在原卡片上变为暖色，不再另外弹出 10/2 分钟气泡。

所有倒计时浮窗都不再固定在右上角，而是每 12 秒在你当前操作的显示器工作区内随机明显换位，避开任务栏。浮窗会增加视觉提醒，但不进入任务栏、不抢键盘焦点，鼠标点击会穿透。选择团队救场、紧急解锁或娱乐再用后，卡片会显示整个有效期和当前时间；娱乐再用明确标注“本次不可续期”，冷静阶段则显示“冷静至”或“娱乐窗口开始”的时间，避免把开始时间误解为结束时间。移动浮窗、切换显示器或修改系统时间都不会给你增加剩余时间，卡片也不会自行发放或延长例外。

0.3.17 还会在所有倒计时卡片外侧显示向外扩散的光晕和四边散射的小粒子。每轮的出现间隔、数量、方向、速度、光环圈数，以及圆点、菱形、光线形态都会变化，不是固定重复播放同一段动画；不过这不能保证你始终不会习惯它。正文尺寸不变，普通卡片四周各预留 48 个界面单位，游戏大卡片各预留 64 个界面单位显示特效；这圈透明区域也会一起参与屏幕避让，不挡住任务栏，鼠标仍可穿透。

动画只在浮窗可见时以每秒 20 帧运行。若你在 Windows 中关闭了动画效果，或启用高对比度，程序会改用静态外圈。光效、位置变化或静态模式都不会改变当前截止时间，也不影响后台门禁与锁屏。

窗口右上角关闭按钮只会把界面收回托盘。若从托盘菜单选择“退出”，当晚电脑保护会停止，程序会先明确提醒并要求确认。

退出后，Chrome 网页保护也会同时停止。正常退出会立即结束桌面保护会话；若程序崩溃或被任务管理器结束，后台最多等待 80 秒单调租约到期后安全放行。重新打开“收尾”会从当前夜间阶段恢复保护，但不会重置已经使用的例外、冷静期或当晚结果记录。

## 三种例外

- 团队救场：每滚动 168 小时最多一次，立即延长 20 分钟；只保留当前游戏和已配置的语音工具。
- 紧急情况：健康、安全或紧急工作可完整解锁 30 分钟；选择原因后立即生效，不限制次数。
- 娱乐再用：先锁屏冷静 10 分钟，再提供一次不可续期的 20 分钟窗口。

例外次数、冷静期和当晚最高阶段会持久保存；重启、重新登录或回拨系统时间不会刷新机会。界面不会用羞辱性红叉或永久连续纪录惩罚未达成的夜晚。

在锁屏页点击团队救场后，程序会先显示“正在向本机服务确认”。接受后页面立即消失并进入
20 分钟救场窗口；不会等到旧的重新锁屏倒计时结束。若当前游戏快照尚未准备好、已经变化，
或 168 小时机会仍在冷却，锁屏页会直接说明原因；快照不可用的拒绝不会消耗救场机会。

## 安装后自检

完成设置后，用下面这份清单确认：

- 通知区域只有一个“收尾”图标；再次运行桌面程序只会打开已有窗口。
- “今晚”页能显示阶段、倒计时和保护状态，而不是持续显示服务不可用。
- Windows“服务”应用中能看到正在运行的 `NightGate Service`，其登录身份为“本地服务”。
- Chrome 设置页显示扩展已连接；若不使用网页保护，已理解其降级提示。
- 添加一个不重要的测试程序规则，确认门禁前启动的实例不会被强杀；正式使用前不要用未保存工作的程序做测试。
- 退出并重新登录后，托盘图标会自动回来，而且仍只有一个实例。

## 故障排查

安装器仍提示系统版本过低：确认使用的是最新 MSI，运行 `winver` 查看“操作系统内部版本”；Windows 11 应不低于 22000。关闭旧安装窗口后重新双击最新文件。

安装时显示未知发布者：这是因为当前 MSI 未签名。核对随包 SHA-256 和文件来源；不一致时立即取消安装。

找不到托盘图标：先展开通知区域，再手工运行桌面程序。若窗口已在后台，第二次运行会唤起它。仍无反应时，注销并重新登录。

界面持续显示服务不可用：电脑会保持安全放行，不会锁死。打开 Windows“服务”，找到 `NightGate Service` 并启动；这一步可能需要管理员确认。也可以重启 Windows。若服务反复停止，请保留 Windows 事件查看器中的错误信息。

Chrome 一直显示降级：确认扩展已启用、加载目录正确，并使用安装时的同一个 Chrome 用户资料；再打开“扩展程序选项”，选择与桌面端相同的网站、保存并允许网站访问权限。隐身保护需要在扩展详情中单独允许。

游戏中看不到倒计时：先检查实际运行的游戏主程序路径是否已添加为“游戏”，并确认已经到了有效最后开局提醒时间；程序不是只凭进程名字判断。普通窗口与无边框全屏可使用 Windows 置顶浮窗，独占全屏游戏则可能把它遮住，可尝试切换为“无边框全屏”或窗口模式。倒计时不可见或移动失败只影响提示，后台门禁、Chrome 拦截和最终锁屏仍按原计划执行。若使用多个不同缩放比例的显示器，尤其是某块屏幕位于另一块左侧或上方时，请确认浮窗连同外侧光效区域在换位后仍完整位于当前前台显示器的工作区，没有遮住任务栏。0.3.17 尚未完成安装后的真实游戏、混合 DPI 实机验收，不能只凭自动化测试保证显示效果。

浮窗有外圈但没有动画：检查 Windows 的动画效果是否关闭，或是否正在使用高对比度。这两种情况下显示静态外圈是预期行为，不影响倒计时和到点锁屏。

程序占用异常：正常情况下应只有一个桌面进程和一个后台服务。若出现多个旧版进程，先从各自托盘菜单正常退出，再运行最新版；不要直接删除安装目录。

## 卸载与恢复

卸载前，如需恢复由“收尾”停用的旧自动关机任务，请先在设置中的“旧任务、历史与隐私”点击“恢复此前停用的旧任务”。随后打开“Windows 设置 → 应用 → 已安装的应用”，找到“收尾 NightGate”并卸载。

如果这项任务最早由 0.3.3 或 0.3.8 停用，升级或重启后可能显示“内容已变化或缺少可信记录”。这不表示任务重新启用；请先看页面是否仍显示“已停用”。旧版没有保存足以安全恢复触发器和权限的完整身份，新版因此不会猜测并自动启用。确实需要恢复时，请在 Windows“任务计划程序”中找到页面显示的同名任务，核对其动作、触发时间和运行账户后手工启用。

卸载会移除程序、后台服务、登录启动项和本程序的 Chrome 本地宿主登记；历史数据库默认保留在 `C:\ProgramData\NightGate`，方便升级或重装后继续使用。卸载不会删除你的个人文件，也不会主动删除旧计划任务。

## 隐私与边界

本机只记录时间、事件类型和网站类别，原始事件最多保留 90 天。你可以在“每周回顾”清除历史；此操作不会清除当前夜间状态和当晚例外。没有云同步，也不保存 Apple 凭据或完整浏览历史。

管理员始终可以停止服务或卸载程序。“收尾”的目标是让冲动多一道步骤，而不是把电脑所有者锁在自己的设备之外。
